server:
  port: 8181

spring:
  application:
    name: instrument-service

multitenancy:
  tenants:
    - identifier: dukes
      enabled: true
    - identifier: beans
      enabled: true
    - identifier: trixie
      enabled: false
